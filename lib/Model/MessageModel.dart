class MessageModel{
  final String imgProfile,tvUserName,tvMessage,tvMinutes,tvNoMessage;

  MessageModel(this.imgProfile, this.tvUserName, this.tvMessage, this.tvMinutes, this.tvNoMessage);
}
List<MessageModel> list = [];
