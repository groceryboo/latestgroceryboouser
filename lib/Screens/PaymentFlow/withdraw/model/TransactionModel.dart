class TransactionModel {
  TransactionModel(this.amount, this.amountColor, this.title, this.subTitle,
      this.storeType, this.date);

  String amount;
  String title;
  String subTitle;
  String storeType;
  String date;
  var amountColor;
}
